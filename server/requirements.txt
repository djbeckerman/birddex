fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
birdnetlib>=0.9.0
# tflite-runtime: birdnetlib bundles the .tflite model but NOT the inference engine.
tflite-runtime>=2.14.0
# librosa: birdnetlib uses it to decode audio segments
# soundfile: librosa's audio I/O backend (requires libsndfile1 system lib, see Dockerfile)
librosa>=0.10.0
soundfile>=0.12.1
# tflite-runtime was compiled against NumPy 1.x — NumPy 2.x breaks it at runtime
# with "A module that was compiled using NumPy 1.x cannot be run in NumPy 2.x"
numpy<2
